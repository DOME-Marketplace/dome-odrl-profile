{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#MarketplaceRef",
  "title": "MarketplaceRef",
  "description": "This reference should always identify a specific marketplace.",
  "$comment": "Generally, the marketplace id is enough for equality comparison.",
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uri-reference",
      "description": "Unique identifier of a related marketplace."
    }
  }
}