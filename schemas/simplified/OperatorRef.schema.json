{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "#OperatorRef",
  "title": "OperatorRef",
  "description": "This reference should always identify a specific access node operator.",
  "$comment": "Generally, the operators id is enough for equality comparison.",
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "type": "string",
      "format": "uri-reference",
      "description": "Unique identifier of a related access node operator."
    }
  }
}